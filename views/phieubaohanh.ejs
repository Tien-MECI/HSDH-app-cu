<!DOCTYPE html>
<html lang="vi">
<head>
<meta charset="UTF-8">
<title>Phiếu Bảo Hành - M.E.C.I</title>
<style>
  @page { size: A4 portrait; margin: 0; }
  body, html {
    margin: 0;
    padding: 0;
    width: 210mm;
    height: 297mm;
    font-family: "Times New Roman", Times, serif;
    font-size: 13px;
    background: #fff;
    -webkit-print-color-adjust: exact;
    print-color-adjust: exact;
  }
  .page {
    position: relative;
    width: 210mm;
    height: 297mm;
  }
  /* Watermark xoay 90 độ vì file gốc là A4 ngang */
  .watermark {
    position: absolute;
    top: 0; left: 0;
    width: 297mm;
    height: 210mm;
    transform: 0;
    opacity: 0.12;
    transform: rotate(90deg) translate(40mm, 40mm);
    transform-origin: center;
    pointer-events: none;
    z-index: 0;
  }
  .watermark img {
    width: 100%;
    height: 100%;
    object-fit: contain;
  }
  .ticket {
    position: absolute;
    width: 198mm;
    height: 140mm;
    left: 6mm;
    border: 2px solid #000;
    padding: 7mm 6mm;
    box-sizing: border-box;
    background: #fff;
    z-index: 1;
  }
  .ticket-top { top: 6mm; }
  .ticket-bottom { top: 151mm; }
  .cut-line {
    position: absolute;
    top: 148mm;
    left: 6mm;
    width: 198mm;
    border-top: 2px dashed #000;
    text-align: center;
    z-index: 2;
  }
  .cut-line span {
    background: #fff;
    padding: 0 15px;
    font-size: 11px;
    font-weight: bold;
  }
  /* Nội dung phiếu */
  .header { text-align: center; line-height: 1.4; }
  .logo { width: 100px; margin-bottom: 4px; }
  .company { font-size: 17px; font-weight: bold; }
  .title { font-size: 23px; font-weight: bold; text-transform: uppercase; margin: 6px 0 4px; }
  .date-example { font-size: 12.5px; font-style: italic; }
  .date-hand { font-style: italic; margin: 10px 0 8px; }
  .serial { font-weight: bold; font-size: 14px; }
  .info td { padding: 3.5px 0; font-size: 13px; }
  .info td:first-child { width: 105px; font-weight: bold; }
  .table-product {
    width: 100%;
    border-collapse: collapse;
    margin: 10px 0 8px;
    font-size: 12.5px;
  }
  .table-product th,
  .table-product td {
    border: 1px solid #000;
    padding: 6px 5px;
    text-align: center;
  }
  .table-product th { background: #f5f5f5; font-weight: bold; }
  .table-product td.left { text-align: left; padding-left: 8px; }
  .conditions {
    font-size: 11.8px;
    line-height: 1.5;
    margin-top: 8px;
  }
  .conditions h3 {
    text-align: center;
    font-size: 14px;
    text-decoration: underline;
    margin: 8px 0 6px;
  }
  .signature {
    display: flex;
    justify-content: space-between;
    margin-top: 15px;
    font-weight: bold;
    font-size: 13px;
  }
  @media print {
    body { -webkit-print-color-adjust: exact; }
    .cut-line span { background: white !important; }
  }
</style>
</head>
<body>
<div class="page">

  <!-- Watermark xoay 90 độ -->
  <% if (watermarkBase64) { %>
    <div class="watermark">
      <img src="<%= watermarkBase64 %>" alt="watermark">
    </div>
  <% } %>

  <!-- PHIẾU 1 -->
  <div class="ticket ticket-top">
    <%- include('phieubaohanh-content') %>
  </div>

  <!-- Đường cắt -->
  <div class="cut-line"><span>✂ CẮT THEO ĐƯỜNG NÀY ✂</span></div>

  <!-- PHIẾU 2 (giống hệt phiếu 1) -->
  <div class="ticket ticket-bottom">
    <%- include('phieubaohanh-content') %>
  </div>

</div>
</body>
</html>