<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Hóa đơn giá trị gia tăng</title>
    <style>
        @page { size: A4 portrait; margin: 0.5cm; }

        body {
            font-family: "Times New Roman", Times, serif;
            margin: 0;
            padding: 0;
            font-size: 12px;
            line-height: 1.2;
            -webkit-print-color-adjust: exact;
            print-color-adjust: exact;
        }

        /* Khung ngoài giống mẫu */
        .container {
            width: 100%;
            max-width: 210mm;
            margin: 0 auto;
            position: relative;
            min-height: 297mm;
            padding: 10px;
            box-sizing: border-box;
            border: 4px solid #2b6ea3; /* màu viền chuẩn mẫu xanh */
            background: white;
        }

        /* watermark (đặt trước nội dung, nhưng z-index thấp) */
            .watermark {
            position: fixed;
            top: 35%;
            left: 50%;
            transform: translate(-50%, -50%);
            z-index: -1;
            opacity: 0.1;
            text-align: center;
        }

        .watermark img {
            width: 100vw;
            /* chiếm 80% chiều rộng trang */
            height: auto;
        }

        /* Header chính */
        .header {
            display: flex;
            align-items: flex-start;
            justify-content: space-between;
            gap: 12px;
            position: relative;
            z-index: 1;
            padding: 6px;
            border-bottom: 2px solid #000;
        }

        .logo { width: 110px; height: auto; }

        .header-center {
            flex: 1;
            text-align: center;
            line-height: 1;
        }

        .invoice-title {
            font-weight: bold;
            font-size: 18px;
            text-transform: uppercase;
            letter-spacing: 0.6px;
        }

        .invoice-sub {
            font-style: italic;
            font-size: 13px;
            margin-top: 2px;
        }

        .invoice-date {
            font-size: 12px;
            margin-top: 6px;
        }

        /* Khối phải: cố định rộng và không wrap */
        .header-right {
            width: 260px;               /* tăng đủ rộng để chứa dòng dài */
            text-align: right;
            white-space: nowrap;        /* KHÔNG xuống dòng */
            font-size: 12px;
            line-height: 1.25;
        }
        .header-right div { margin-bottom: 3px; }

        /* Các vùng (seller / buyer) có khung riêng */
        .section {
            border: 1px solid #000;
            padding: 8px;
            margin-top: 8px;
            background: #fff;
            z-index: 1;
        }

        .section + .section { margin-top: 10px; }

        /* Bảng chi tiết */
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 8px;
            z-index: 1;
        }

        th, td {
            border: 1px solid #000;
            padding: 6px;
            vertical-align: middle;
            font-size: 12px;
        }

        thead th { font-weight: 700; text-align: center; }

        /* Đặt tỷ lệ cột giống mẫu */
        .col-stt { width: 4%; }
        .col-desc { width: 42%; text-align:left; padding-left:6px; }
        .col-unit { width: 7%; }
        .col-qty { width: 8%; }
        .col-unitprice { width: 12%; text-align:right; }
        .col-amount { width: 12%; text-align:right; }
        .col-tax-percent { width: 6%; }
        .col-tax-amt { width: 8%; text-align:right; }
        .col-total { width: 12%; text-align:right; }

        tfoot td { font-weight: 700; }

        /* Bảng tổng hợp phía dưới: giống mẫu, viền rõ */
        .summary-table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 6px;
        }
        .summary-table th, .summary-table td {
            border: 1px solid #000;
            padding: 6px;
            font-size: 12px;
            text-align: center;
        }

        .amount-in-words { margin-top: 12px; font-style: italic; }

        .signature {
            display: flex;
            justify-content: space-between;
            margin-top: 26px;
            z-index: 1;
        }
        .signature div { width: 45%; text-align: center; }

        .footer-note {
            text-align: center;
            font-size: 11px;
            margin-top: 10px;
            font-style: italic;
        }

        /* Khi in, đảm bảo watermark và box vẫn hiện */
        @media print {
            .watermark { opacity: 0.8 !important; }
            .container { border-width: 3px; }
        }
        table {
  width: 100%;
  border-collapse: collapse;
  margin-top: 8px;
}

th, td {
  border: 1px solid #000;
  padding: 5px;
  font-size: 12px;
  text-align: center;
  vertical-align: middle;
}

thead th {
  font-weight: bold;
}

thead th em {
  font-weight: normal;
  font-style: italic;
  font-size: 11px;
}

tbody td {
  font-size: 12px;
}

    </style>
</head>
<body>
    <div class="container">
        <!-- watermark (data uri) -->
        <% if (watermarkBase64) { %>
            <img class="watermark" src="<%= watermarkBase64 %>" alt="watermark">
        <% } %>

        <!-- Header -->
        <div class="header">
            <!-- logo -->
            <div style="width:120px;">
                <% if (logoBase64) { %>
                    <img class="logo" src="<%= logoBase64 %>" alt="logo">
                <% } %>
            </div>

            <!-- center title -->
            <div class="header-center">
                <div class="invoice-title">HÓA ĐƠN GIÁ TRỊ GIA TĂNG</div>
                <div class="invoice-sub">(VAT INVOICE)</div>
                <div class="invoice-date">
                    Ngày <%= today.getDate() %> tháng <%= today.getMonth()+1 %> năm <%= today.getFullYear() %>
                </div>
            </div>

            <!-- right block -->
            <div class="header-right">
                <div>Mẫu số (<em>Form No.</em>): <strong>1</strong></div>
                <div>Ký hiệu (<em>Serial No.</em>): <strong>C25TMC</strong></div>
                <div>Mã của Cơ quan thuế (<em>Tax Authority Code</em>): <strong><%= order && order.taxCode ? order.taxCode : "" %></strong></div>
                <div>Số (<em>Invoice No.</em>): <strong><%= order && order.madh ? order.madh : "00000000" %></strong></div>
            </div>
        </div>

        <!-- Seller section -->
        <div class="section">
            <strong>Đơn vị bán hàng (Seller):</strong> CÔNG TY CỔ PHẦN CÔNG NGHIỆP M.E.C.I<br>
            <strong>Mã số thuế (Tax Code):</strong> 0105273501<br>
            <strong>Địa chỉ (Address):</strong> Số 164 đường Nguyễn Trãi, Phường Thanh Xuân, TP Hà Nội, Việt Nam<br>
            <strong>Số điện thoại (Tel):</strong> 0902.227.007 - 0982.934.876 &nbsp; <strong>Website:</strong> www.meci.vn<br>
            <strong>Số tài khoản (Account No.):</strong> 22011686868 tại Ngân hàng TMCP Tiên Phong - Chi nhánh Hoài Đức
        </div>

        <!-- Buyer section -->
        <div class="section">
            <strong>Họ tên người mua hàng (Buyer):</strong> ......................................................................<br>
            <strong>Tên đơn vị (Co. name):</strong> <%= order && order.companyName ? order.companyName : "" %><br>
            <strong>Địa chỉ (Address):</strong> <%= order && order.address ? order.address : "" %><br>
            <strong>Số tài khoản (Account No.):</strong> .................................................<br>
            <strong>Hình thức thanh toán (Pay. method):</strong> Tiền mặt/Chuyển khoản &nbsp;
            <strong>Mã số thuế (Tax Code):</strong> <%= order && order.taxCode ? order.taxCode : "" %>
        </div>

        <!-- Products table -->
        <table>
            <thead>
  <tr>
    <th rowspan="2" style="width: 4%;">STT<br><em>(No.)</em></th>
    <th rowspan="2" style="width: 36%;">Tên hàng hóa, dịch vụ<br><em>(Description)</em></th>
    <th rowspan="2" style="width: 6%;">ĐVT<br><em>(Unit)</em></th>
    <th rowspan="2" style="width: 8%;">SL<br><em>(Quantity)</em></th>
    <th rowspan="2" style="width: 10%;">Đơn giá<br><em>(Unit price)</em></th>
    <th rowspan="2" style="width: 12%;">Tiền chưa thuế<br><em>(Amount)</em></th>
    <th colspan="2" style="width: 14%;">Thuế GTGT<br><em>(VAT)</em></th>
    <th rowspan="2" style="width: 10%;">Thành tiền sau thuế<br><em>(Total Amount)</em></th>
  </tr>
  <tr>
    <th style="width: 5%;">%</th>
    <th style="width: 9%;">Tiền thuế<br><em>(VAT Amount)</em></th>
  </tr>
</thead>

            <tbody>
                <% if (products && products.length) { %>
                    <% products.forEach(function(p, idx) { %>
                        <tr>
                            <td style="text-align:center;"><%= p.stt || (idx+1) %></td>
                            <td style="text-align:left; padding-left:6px;"><%= p.description %></td>
                            <td><%= p.unit %></td> 
                            <td><%= formatNumber1(p.quantity) %></td>
                            <td style="text-align:right;"><%= formatNumber1(p.amount) %></td>
                            <td style="text-align:right;"><%= formatNumber1(p.unitPrice) %></td>
                            <td style="text-align:center;"><%= p.taxRate %>%</td>
                            <td style="text-align:right;"><%= formatNumber1(p.taxAmount) %></td>
                            <td style="text-align:right;"><%= formatNumber1(p.totalAmount) %></td>
                        </tr>
                    <% }) %>
                <% } else { %>
                    <tr><td colspan="9" style="text-align:center; padding:14px;">Không có dữ liệu</td></tr>
                <% } %>
            </tbody>
            <tfoot>
                <tr>
                    <td colspan="5" style="text-align:right;"><strong>Tổng cộng (Amount):</strong></td>
                    <td style="text-align:right;"><strong><%= formatNumber1(totalAmountBeforeTax) %></strong></td>
                    <td></td>
                    <td style="text-align:right;"><strong><%= formatNumber1(totalTax) %></strong></td>
                    <td style="text-align:right;"><strong><%= formatNumber1(totalAmount) %></strong></td>
                </tr>
            </tfoot>
        </table>

        <!-- Summary table under -->
        <table class="summary-table">
            <thead>
                <tr>
                    <th>Tổng hợp (Total)</th>
                    <th>Thuế suất (Tax rate)</th>
                    <th>Tiền trước thuế (Amount)</th>
                    <th>Tiền thuế GTGT (Tax Amount)</th>
                    <th>Tổng thanh toán (Total Amount)</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td style="text-align:left;">Tổng tiền chịu thuế suất (0%)</td>
                    <td>0%</td>
                    <td style="text-align:right;"><%= formatNumber1(summary.totalAmount0 || 0) %></td>
                    <td style="text-align:right;">0</td>
                    <td style="text-align:right;"><%= formatNumber1(summary.totalAmount0 || 0) %></td>
                </tr>
                <tr>
                    <td style="text-align:left;">Tổng tiền chịu thuế suất (8%)</td>
                    <td>8%</td>
                    <td style="text-align:right;"><%= formatNumber1(summary.totalAmount8 || 0) %></td>
                    <td style="text-align:right;"><%= formatNumber1(summary.totalTax8 || 0) %></td>
                    <td style="text-align:right;"><%= formatNumber1((summary.totalAmount8||0) + (summary.totalTax8||0)) %></td>
                </tr>
                <tr>
                    <td style="text-align:left;">Tổng tiền chịu thuế suất (10%)</td>
                    <td>10%</td>
                    <td style="text-align:right;"><%= formatNumber1(summary.totalAmount10 || 0) %></td>
                    <td style="text-align:right;"><%= formatNumber1(summary.totalTax10 || 0) %></td>
                    <td style="text-align:right;"><%= formatNumber1((summary.totalAmount10||0) + (summary.totalTax10||0)) %></td>
                </tr>
                <tr>
                    <td colspan="2" style="text-align:center;"><strong>Tổng cộng</strong></td>
                    <td style="text-align:right;"><strong><%= formatNumber1(totalAmountBeforeTax) %></strong></td>
                    <td style="text-align:right;"><strong><%= formatNumber1(totalTax) %></strong></td>
                    <td style="text-align:right;"><strong><%= formatNumber1(totalAmount) %></strong></td>
                </tr>
            </tbody>
        </table>

        <!-- Amount in words -->
        <div class="amount-in-words">
            Tổng số tiền viết bằng chữ (Amount in words): <strong><%= numberToWords(totalAmount) %></strong>
        </div>

        <!-- Signatures -->
        <div class="signature">
            <div>
                <strong>Người mua hàng (Buyer)</strong><br><br><br>
                (Ký, ghi rõ họ tên)
            </div>
            <div>
                <strong>Người bán hàng (Seller)</strong><br><br><br>
                (Ký, ghi rõ họ tên)
            </div>
        </div>

        <div class="footer-note">
            (Cần kiểm tra đối chiếu khi lập, giao, nhận hóa đơn)
        </div>
    </div>
</body>
</html>
